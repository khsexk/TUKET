doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
    body
    h1 
        header  💰💰Welcome TU MARKET💰💰
        div#clock
            header
    main           
        div#welcome
            form
                input(type="text", value=`${nickname}`, required)#nick
                input(type="text", value = `${room}`, hidden = true)#roomNumber
                button 채팅방 입장

        div#room
            ul
            h3 
            form
                input(type="text", placeholder="메시지 입력하세요", required)
                button 보내기
            form(action='/post' method='get')
                input(type="submit", value = "채팅방 나가기")
            h3 채팅방 목록
            ul#roomul
    script(src="/socket.io/socket.io.js")
    script(src="/public/js/front.js")